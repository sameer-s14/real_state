export enum DB_CONFIGS_KEYS {
  DB_NAME = 'DB_NAME',
  DB_USER = 'DB_USER',
  DB_PASSWORD = 'DB_PASSWORD',
  DB_HOST = 'DB_HOST',
  DB_DIALECT = 'DB_DIALECT',
  DB_PORT = 'DB_PORT',
}
export enum ENV_KEY {
  NODE_ENV = 'NODE_ENV',
  PORT = 'PORT',
}

export enum SWAGGER_DOCS {
  TITLE = 'Real State App',
  DESCRIPTION = 'This Documentation contains apis for real state app',
  VERSION = '1.0',
  WEBSITE_TITLE = 'Real State App Swagger Documentation',
}

export enum SWAGGER_TAGS {
  MISC = 'Misc',
  PROPERTY = 'Property',
  AUTH = 'Auth',
}

export enum ENVIRONMENTS {
  DEVELOPMENT = 'development',
  PRODUCTION = 'production',
}
